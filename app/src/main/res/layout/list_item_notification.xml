<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="?attr/listPreferredItemHeightLarge"
    android:padding="8dp"
    android:background="#ffffff"
    >

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/textViewTitle"
        android:text="This is a title. This should be a title."
        android:textColor="@color/colorSecondary"
        android:layout_marginBottom="3dp"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/textViewBody"
        android:text="Ruby on Rails Web Services and Integration with MongoDB. Ruby on Rails Web Services and Integration with MongoDB. Ruby on Rails Web Services and Integration with MongoDB. "
        android:layout_below="@+id/textViewTitle"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:maxLines="2"
        android:textColor="#000000"
        android:textSize="5pt"
        android:layout_marginBottom="3dp"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="6h ago"
        android:id="@+id/textViewTime"
        android:layout_alignParentBottom="true"
        android:layout_below="@+id/textViewBody"
        android:layout_alignRight="@+id/textViewBody"
        android:layout_alignEnd="@+id/textViewBody"
        android:alpha="0.5"
        android:textSize="5pt"
        />

</RelativeLayout>